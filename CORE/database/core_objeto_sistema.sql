  CREATE TABLE "CORE_OBJETO_SISTEMA" 
   (	"ID_OBJETO_SISTEMA" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"DESC_OBJETO_SISTEMA" VARCHAR2(255), 
	"TIPO_OBJETO_SISTEMA" VARCHAR2(50), 
	"COD_APP_ID" VARCHAR2(30), 
	"COD_PAGINA" VARCHAR2(30), 
	"ICONE" VARCHAR2(255), 
	"ID_OBJETO_SISTEMA_PAI" NUMBER(*,0), 
	"SEQUENCIA" NUMBER, 
	"SITUACAO" VARCHAR2(1), 
	"USUARIO_CRIACAO" VARCHAR2(100), 
	"DATA_CRIACAO" DATE, 
	"USUARIO_ATUALIZACAO" VARCHAR2(100), 
	"DATA_ATUALIZACAO" DATE, 
	"GRUPO_APP" VARCHAR2(200), 
	 CONSTRAINT "CORE_OBJETO_SISTEMA_PK" PRIMARY KEY ("ID_OBJETO_SISTEMA")
  USING INDEX  ENABLE, 
	 CONSTRAINT "CORE_OBJETO_SISTEMA_UQ" UNIQUE ("COD_APP_ID", "COD_PAGINA")
  USING INDEX  ENABLE
   ) ;
/

CREATE OR REPLACE EDITIONABLE TRIGGER "CORE_OBJETO_SISTEMA_BIU" 
    before insert or update
    on CORE_OBJETO_SISTEMA
    for each row
begin
    if inserting then
        :new.DATA_CRIACAO    := sysdate;
        :new.USUARIO_CRIACAO := coalesce(sys_context('APEX$SESSION','APP_USER'),user);
    end if;
    :new.DATA_ATUALIZACAO    := sysdate;
    :new.USUARIO_ATUALIZACAO := coalesce(sys_context('APEX$SESSION','APP_USER'),user);
end CORE_OBJETO_SISTEMA_biu;
/
ALTER TRIGGER "CORE_OBJETO_SISTEMA_BIU" ENABLE;